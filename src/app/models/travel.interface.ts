export interface Travel {
    id: number,    
    rating: number,
    weather: string,
    timezone: string,
    native_language: string,
    images: string[],
    local_currency: string,
    title: string,
    path: string
}